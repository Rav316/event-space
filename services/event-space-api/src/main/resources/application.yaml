app:
  static-content:
    path: ${STATIC_CONTENT_PATH}
  security:
    jwt:
      access-token:
        secret: ${JWT_ACCESS_SECRET}
        expiration: 900000 # 15 minutes
      refresh-token:
        secret: ${JWT_REFRESH_SECRET}
        expiration: 2592000000 # 30 days
      issuer: event space
  account:
    deletion-confirmation-phrase: "удалить мой аккаунт"
  messaging:
    rabbit:
      exchange: event.notifications
      routing-key: event.created
      queue: event.notifications.new-events
      reminder-routing-key: event.reminder
      reminder-queue: event.notifications.reminders
  reminder:
    zone: Europe/Moscow
    lead-time: PT24H
    batch-size: 100
    max-attempts: 5
    poll-ms: 60000
  metrics:
    username: ${PROMETHEUS_USERNAME}
    password: ${PROMETHEUS_PASSWORD}

spring:
  application:
    name: event-space-api
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/event_space_db}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  threads:
    virtual:
      enabled: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
      base-path: /api/actuator
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true
    tags:
      application: ${spring.application.name}
springdoc:
  swagger-ui:
    operations-sorter: method
    tags-sorter: alpha
    deep-linking: true
    path: /api/docs/swagger
  api-docs:
    path: /api/docs/openapi